\# Git的八个指令总结

1\. git add <文件名>  

&nbsp;  功能：将指定文件的修改添加到“暂存区”（为后续提交做准备）；若需添加所有修改的文件，可使用 `git add .`（注意“.”代表当前目录）。



2\. git commit -m "提交备注"  

&nbsp;  功能：将暂存区的修改提交到“本地仓库”；`-m` 是必填参数，用于添加清晰的提交说明（如“添加git add与git commit指令及说明”），避免提交后无法识别修改内容，这个需要格外注意。



3\. git push origin <分支名>  

&nbsp;  功能：将本地分支的提交推送到“远程仓库”（即你Fork的GitHub个人副本仓库）；例如推送LXT分支到远程，命令为 `git push origin LXT`，origin 是远程别名。



4\. git branch  

&nbsp;  功能：查看本地所有分支，当前所在分支前会显示 `\*` 标记；若需查看远程分支，可添加参数 `-r`，命令为 `git branch -r`；若需创建新分支，格式为 `git branch <新分支名>`（如 `git branch LXT`）啊哈哈哈哈就是这样。



5\. git checkout <分支名>  

&nbsp;  功能：切换到指定的本地分支，例如切换到LXT分支的命令为`git checkout LXT`；若需“创建并同时切换分支”，可使用`git checkout -b <新分支名>`（如你之前创建LXT分支的命令）。



6\. git pull  

&nbsp;  功能：拉取远程仓库对应分支的最新代码到本地，保持本地与远程代码同步；若本地分支与远程分支关联，直接执行 `git pull` 即可，避免后续推送时出现代码冲突。



7\. git status  

&nbsp;  功能：查看当前工作区的文件状态，包括“已修改但未添加到暂存区的文件”“已添加到暂存区但未提交的文件”等；执行后终端会用不同颜色标注状态，帮助确认操作进度。



8\. git log  

&nbsp;  功能：查看本地仓库的提交历史，包括每次提交的作者、时间、提交ID、提交备注；若需简化显示（只看关键信息），可添加参数 `--oneline`，命令为 `git log --oneline`。

